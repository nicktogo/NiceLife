<Page
    x:Class="NiceLife.Weather.WeatherFlipView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NiceLife.Weather"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:NiceLife.Weather.Database">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <FlipView x:Name="WeatherFlip" ItemsSource="{x:Bind weathers, Mode=OneWay}">
            <FlipView.ItemTemplate>
                <DataTemplate x:DataType="data:WeatherModel">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="10" HorizontalAlignment="Center">
                            <TextBlock Text="{x:Bind realtimeDetail.CountyName}" VerticalAlignment="Top" />
                            <TextBlock Text="{x:Bind realtimeDetail.UpdateTime}" VerticalAlignment="Center" />
                            <TextBlock Text="{x:Bind realtimeDetail.RealtimeTemp}" VerticalAlignment="Bottom" />
                        </Grid>

                        <GridView Grid.Row="1" ItemsSource="{x:Bind forecasts}" Margin="10"
                                  IsItemClickEnabled="True" ItemClick="GridView_ItemClick">
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="data:Forecast">
                                    <StackPanel HorizontalAlignment="Center" Margin="10">
                                        <TextBlock FontSize="18" Text="{x:Bind date}" HorizontalAlignment="Center"/>
                                        <Image Width="150" Height="auto" />
                                        <Grid>
                                            <TextBlock FontSize="16" Text="{x:Bind low}" HorizontalAlignment="Left"/>
                                            <TextBlock FontSize="16" Text="{x:Bind hight}" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </StackPanel>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</Page>

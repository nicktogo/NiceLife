<Page
    x:Class="NiceLife.Set"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NiceLife"
    xmlns:d ="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc ="http://schemas.openxmlformats.org/markup-compatibility/2006"
   >

    <Grid Background="#FFE4E1" Margin="0,0,0,0" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"/>
            <RowDefinition Height="90*"/>

        </Grid.RowDefinitions>

        <ListView Name="listView1"  FontSize="28" Margin="131,0,130,10" Grid.Row="1" >
            <Frame x:Name="frame" Content=""/>
        </ListView>

        <AppBarButton x:Name="appBarButton_add" Click="appBarButton_add_Click" Grid.Row="0" HorizontalAlignment="Left" Icon="Add" Label="添加" Margin="1088,9,0,36" VerticalAlignment="Stretch" ToolTipService.ToolTip="new a ColorLable"/>
        <AppBarButton x:Name="appBarButton_save" Click="appBarButton_save_Click" Grid.Row="0" HorizontalAlignment="Left" Icon="Save" Label="编辑" Margin="1140,8,0,0" VerticalAlignment="Top" ToolTipService.ToolTip="new a ColorLable" Height="59"/>
        <Frame x:Name="frame_color" Content="" HorizontalAlignment="Left" Margin="562,0,0,0" VerticalAlignment="Top" Height="269" Width="588" Grid.Row="1"/>

    </Grid>
</Page>

<Page
    x:Class="NiceLife.SchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NiceLife"
    xmlns:area="using:NiceLife.Schedule"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FFAEB9">
        <StackPanel Background="#FFE4E1"  HorizontalAlignment="Right" Height="136" VerticalAlignment="Top" Width="1280" Orientation="Horizontal">
            <AppBarButton x:Name="add" Click="add_Click" HorizontalAlignment="Left" Icon="Accept" Label="add" Margin="1000,30,0,0" VerticalAlignment="Top" Width="100" Height="100" FontSize="13.333"/>
            <AppBarButton x:Name="select" Click="select_Click" HorizontalAlignment="Stretch" Icon="Accept" Label="select" VerticalAlignment="Stretch"   Margin="10,30,0,0"/>
            <AppBarButton x:Name="set" Click="set_Click" HorizontalAlignment="Stretch" Icon="Accept" Label="set" VerticalAlignment="Stretch"  Margin="20,30,0,0"/>


        </StackPanel>

        <TextBlock FontSize="36" Text="Mail Page" Margin="0,0,0,12"/>

        <Frame Name="ScheduleFrame" HorizontalAlignment="Left" Margin="0,177,0,0" VerticalAlignment="Top" Height="543" Width="1280">

        </Frame>



    </Grid>
</Page>
